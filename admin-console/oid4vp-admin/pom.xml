<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>org.keycloak</groupId>
    <version>0.2.0-SNAPSHOT</version>
    <artifactId>oid4vp-admin-ui</artifactId>
    <name>OID4VP Admin Console</name>
    <description>The Keycloak administration console, rebuilt with a page for the OID4VP identity provider.</description>

  <licenses>
    <license>
      <name>The Apache License, Version 2.0</name>
      <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

    <properties>
        <node.version>v20.13.0</node.version>
    </properties>
    <build>
        <resources>
            <resource>
                <directory>maven-resources</directory>
            </resource>
            <resource>
                <directory>dist</directory>
                <targetPath>theme/oid4vp/admin/resources</targetPath>
            </resource>
            <!-- Third-party attributions for the bundled front-end dependencies, produced by
                 `pnpm run notices`. The bundle inlines them, so the jar distributes them even
                 though this repository does not; the file is generated rather than written by
                 hand so it cannot drift from the lockfile. -->
            <resource>
                <directory>generated</directory>
                <targetPath>META-INF</targetPath>
            </resource>
        </resources>

        <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>
        <version>3.3.1</version>
        <executions>
          <!-- A jar dropped into providers/ is separated from its repository for good: the
               licence has to travel inside it, which is what Apache 2.0 section 4(d) asks for. -->
          <execution>
            <id>copy-legal-files</id>
            <phase>process-resources</phase>
            <goals><goal>copy-resources</goal></goals>
            <configuration>
              <outputDirectory>${project.build.outputDirectory}/META-INF</outputDirectory>
              <resources>
                <resource>
                  <directory>${project.basedir}/../..</directory>
                  <includes>
                    <include>LICENSE</include>
                    <include>NOTICE</include>
                  </includes>
                </resource>
              </resources>
            </configuration>
          </execution>
        </executions>
      </plugin>
            <plugin>
                <groupId>com.github.eirslett</groupId>
                <artifactId>frontend-maven-plugin</artifactId>
                <version>1.15.0</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>install-node-and-npm</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>npm-install</id>
                        <goals>
                            <goal>npm</goal>
                        </goals>
                        <configuration>
                            <arguments>install</arguments>
                            <workingDirectory>${maven.multiModuleProjectDirectory}</workingDirectory>
                        </configuration>
                    </execution>
                    <execution>
                        <id>npm-build</id>
                        <goals>
                            <goal>npm</goal>
                        </goals>
                        <configuration>
                            <arguments>run build</arguments>
                        </configuration>
                    </execution>
                </executions>
                <configuration>
                    <nodeVersion>${node.version}</nodeVersion>
                    <installDirectory>${maven.multiModuleProjectDirectory}</installDirectory>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
